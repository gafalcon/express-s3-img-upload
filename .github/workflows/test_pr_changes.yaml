name: PR changes
on:
  pull_request_target:
    types:
      - closed
  branches:
    - 'test'
jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run : |
        echo "The PR was merged"
